const mockUser = {
  _id: "user123",
  userGames: []
};

mockUser.save = jest.fn().mockResolvedValue(mockUser);

const User = {
  exists: jest.fn(),
  findOneAndUpdate: jest.fn(),
  findById: jest.fn().mockResolvedValue(mockUser),
};

module.exports = {User};
module.exports.__mockUser = mockUser;
