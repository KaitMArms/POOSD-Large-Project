const mockUser = {
  _id: "user123",
  userGames: [],
  save: jest.fn().mockResolvedValue(true),
};

module.exports = {
  User: {
    findById: jest.fn().mockResolvedValue(mockUser),
  },
  __mockUser: mockUser
};
